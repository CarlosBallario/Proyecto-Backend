<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body class="container">
    <h1 class="mt-5">Detalle del equipo</h1>
    <div class="card mt-3">
        <div class="card-body">
            <h5 class="card-title">{{title}}</h5>
            <p class="card-text"><strong>Id:</strong> {{_id}}</p>
            <p class="card-text"><strong>Description:</strong> {{description}}</p>
            <p class="card-text"><strong>Code:</strong> {{code}}</p>
            <p class="card-text"><strong>Price:</strong> ${{price}}</p>
            <p class="card-text"><strong>Stock:</strong> {{stock}}</p>
            <p class="card-text"><strong>Category:</strong> {{category}}</p>
            
        </div>
    </div>
    <a href="/" class="btn btn-primary mt-3">Regresa al listado de equipos</a>
</body>
</html>